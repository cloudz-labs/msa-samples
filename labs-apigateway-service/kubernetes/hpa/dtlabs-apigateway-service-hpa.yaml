apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: dtlabs-apigateway-service
  namespace: default

spec:
  maxReplicas: 10
  minReplicas: 1
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: dtlabs-apigateway-service
  targetCPUUtilizationPercentage: 50      